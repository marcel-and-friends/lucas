syntax = "proto3";

message HeaterControl {
  message Start {
    int32 target_temperature = 1;
    int32 duration_ms = 2;
    float p = 3;
    float i = 4;
    float d = 5;
  }

  message Stop {}

  oneof tag {
    Start start = 1;
    Stop stop = 2;
  }
}
