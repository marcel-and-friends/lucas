syntax = "proto3";

import "proto/firmware/Alarm.proto";
import "proto/firmware/HeatingReport.proto";

message FirmwareEvent {
  oneof tag {
    HeatingReport heating_report = 1;
    Alarm alarm = 2;
  }
}
